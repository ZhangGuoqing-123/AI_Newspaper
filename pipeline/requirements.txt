openai>=1.40
python-dotenv>=1.0
edge-tts>=6.1
requests>=2.31
# 小硅口播视频（屏幕脸 + 声波驱动）
Pillow>=10.0
numpy>=1.24
imageio-ffmpeg>=0.5   # 自带 ffmpeg 二进制，解码音频 + 合成 MP4，无需系统装 ffmpeg
chromadb>=0.5         # 向量检索：持久化本地向量库，配合 SiliconFlow BAAI/bge-m3 embedding
fastapi>=0.110        # server.py：把 chat_agent 包装成 SSE 接口给前端用
uvicorn[standard]>=0.30
supabase>=2.0         # 持久化存储：信源列表 + 推文库（走 REST API，绕开本地代理对直连的干扰）
apscheduler>=3.10     # 定时任务：每 15 分钟自动巡检信源、爬取最新推文
